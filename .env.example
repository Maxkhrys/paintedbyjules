# Canonical public URL. Vercel's production URL is used when omitted.
NEXT_PUBLIC_SITE_URL=

# Optional delivery integrations. Site stays in preview mode when omitted.
COMMISSION_WEBHOOK_URL=
CONTACT_WEBHOOK_URL=
